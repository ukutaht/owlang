module test {
  fn two_and_four() {
    (2, 4)
  }

  fn nested() {
    ((1, 2), (3, 4))
  }

  fn main() {
    assert_eq(tuple_nth(two_and_four(), 0), 2)
    assert_eq(tuple_nth(two_and_four(), 1), 4)
    assert_eq(tuple_nth(tuple_nth(nested(), 1), 0), 3)
  }
}

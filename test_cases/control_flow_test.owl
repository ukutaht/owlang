module control_flow_test {
  fn gimme_one() {
    if true {
      1
    }
  }

  fn two_in_else() {
    if false {
      1
    } else {
      2
    }
  }

  fn main() {
    owl_unit:assert_eq(gimme_one(), 1)
    owl_unit:assert_eq(two_in_else(), 2)
  }
}

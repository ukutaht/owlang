module owl_unit {
  fn assert(value) {
    if !value {
      exit(1)
    }
  }

  fn refute(value) {
    if value {
      exit(1)
    }
  }

  fn assert_eq(left, right) {
    if left != right {
      exit(1)
    }
  }

  fn refute_eq(left, right) {
    if left == right {
      exit(1)
    }
  }
}
